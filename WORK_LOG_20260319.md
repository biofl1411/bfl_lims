# 작업 일지 2026-03-19

## 시험 결재 관련

### 시험일지 식품유형 적용 구조 수정
- 기존 `_appliedFoodTypes` 배열 방식 → 기록서 양식 수집기와 동일하게 **식품유형별 개별 문서 복사 생성** 방식으로 변경
- 카운트 표시: 적용된 문서 수 / 적용 가능한 전체 수

### 결과 입력 연동
- 표준물질정보/표준용액농도 시험일지 값 자동 매칭
- 표준용액 적용 기간 만료 시 기록서 작성 차단 (시험일지에서 갱신 필요)
- 값 변경 시 확인 모달 + 적용 기간 질문
- 소비기한 라벨 통일
- 검사목적 필드 접수 데이터 매칭

### 소수점/처리방식 다중 규칙
- 시험일지에서 규칙 추가/삭제 가능 (1규칙, 2규칙, 3규칙...)
- 결과 입력 시 규칙 순서대로 적용
- 전체 자릿수 결과 + 규칙 적용 결과 이중 표시

### 시험 결재 설정 페이지 추가
- 시험 결재 > 설정 > "시험 결재" 탭 신규 생성 (`approvalSettings.html`)
- 결재 단계/역할/옵션 설정, 결재 흐름 미리보기

## 접수 관련

### 접수번호 중복 방지
- 더블클릭 방지 + 중복 감지 재할당 + 연번 건너뜀 안내

### 접수 등록 UI 개선
- 시료수 입력폼 위치 이동 (접수번호 할당 앞으로)
- 긴급여부 버튼식으로 변경 (일반/긴급/시간긴급/미생물긴급)
- 시간긴급 시간 입력폼 추가

### 접수 데이터 품목코드 백필
- 기존 접수 데이터에 mfdsProductCode가 없는 시료에 foodType 기반으로 코드 매칭하여 업데이트

## 검사 관리

### 참고용 항목 드래그 앤 드롭 순서 변경
- SortableJS 적용, 기존 숫자 입력 방식 대체

### 시험항목 사용여부 전체 페이지 일관 반영
- 시험항목 탭에서 비활성화한 항목이 수수료 매핑 등 다른 페이지에서도 동일하게 반영

### 수수료 부가세 적용
- mfdsTemplates 전체 시험항목 수수료에 부가세(×1.1) 적용 (2,656건)
- 부동소수점 오차 보정 (ceil → round)

### 검사목적/검사분야 하드코딩 제거
- HTML select option 하드코딩 → Firestore 동적 로드로 변경

## OCR 대폭 개선 ★

### 8종 의뢰서 양식 자동 판별
- 식품/축산/RT-PCR/ELISA/방사능/물질/할랄비건/소비기한 자동 판별
- 양식별 분야·검사목적 자동 선택

### OCR 방식 최종 확정: Clova General OCR + Claude 구조화
- **1단계**: Naver Clova General OCR → 정확한 한글/숫자 텍스트 추출
- **2단계**: Claude → Clova 텍스트 + 원본 이미지 동시 분석 → 36개 필드 JSON 구조화
- Clova가 텍스트를 정확히 읽고, Claude가 이미지와 대조하여 누락/불확실한 부분 보완

### 36개 필드 프롬프트 보강
- 상호, 대표자, 소재지, 전화, 팩스, 업종, 검사목적, 성적서구분, 수령방법, 수령주소
- 제품명, 식품유형, 단서조항, 포장단위, 검체량, 운반상태, 제조일자, 소비기한, 시험의뢰항목
- 메모, 긴급협의일, 시료반환, 수수료, 납부구분, 세금계산서, 발행요청일, 입금예정일, 입금자명
- 회사명/사업자번호, 계산서메일주소, 첨가물제외, 미생물제외, 개인정보동의, 서명일자, 접수자, 의뢰인

### OCR 드래그 앤 드롭 파일 업로드
- 접수 등록 상단 OCR 영역 확대, 파일 드래그 앤 드롭 지원

### 담당자별 손글씨 보정 사전 시스템 ★
- 접수 완료 시 OCR 원본값과 사용자 수정값 비교
- 글자 단위 차이 분석 (초성/중성/종성 분해)
- Firestore `ocrCorrections` 컬렉션에 담당자별 보정 이력 저장
- 다음 OCR 시 해당 담당자의 손글씨 패턴을 Claude 프롬프트에 자동 주입
- 사용할수록 보정 사전이 축적되어 정확도 향상

## 서버 관리

### receipt_api 안정화
- debug 모드 비활성화 (git pull 시 프로세스 종료 방지)
- watchdog cron 등록 (5분마다 receipt_api, ocr_proxy, api_server 프로세스 체크, 자동 재시작)

### nginx 설정 수정
- `/api/ocr/` 경로를 OCR 프록시(5002)로 프록시 추가

### Claude API Key 복구
- 서버 교체 후 `~/.claude_api_key` 파일 누락 → `.env`의 ANTHROPIC_API_KEY에서 복원

### Clova OCR 도메인 신규 생성
- General OCR 도메인 (50990) 생성 + API Gateway 연동
- 새 시크릿키/URL 코드에 적용
