# 작업 일지 — 2026-03-24

## 1. 접수 데이터 보호 전면 수정

### 편집 모드 시험항목 초기화 근본 수정
- **근본 원인**: `onTestPurposeChange()` 내 `clearInspectionItems()` 호출
  - 편집 모드에서 `_filledFromHistory=false` → 검사목적 설정 시 시험항목 초기화
  - 타이밍: 시험항목 복원(0ms) → clearInspectionItems(500ms) → 재로드(2000ms)
- **수정**: `_filledFromHistory = true` 설정으로 초기화 차단
- 검사목적 모드 불일치 필터링: P코드/B코드 자동 분류

### 27개 필드 데이터 손실 방지
- loadReceiptForEdit에 누락 필드 추가:
  - 성적서 발송: mailAddrTypes, mailZipcode, mailAddr1/2, faxTypes, emailCc
  - 청구서: billFax, billMobile
  - 팀별 메모: memoCustomer/Quality/Finance/Marketing/Sales
  - 접수 공지: selectedNotices, noticeRemark, noticeMemo
  - 수수료/결제: 13개 필드 (_editFeeData 보관)
  - 검사정보: relatedLaw, documentNo
  - 첨부파일: _editExistingFiles 보관
- 병합 로직: 빈 배열/객체/숫자0 → 기존 값 보존
- files 병합: 새 파일 없으면 기존 유지

### 추가 수정
- 접수형태: syncInlineUI('formType') → syncInlineUI('form') 키 수정
- 지부/라벨: collectReceiptData()에 branchId/branchName/labels 추가
- 공지 메모: notice-memo-box에서 직접 읽기 (3개 다른 id 통합)
- serial 모드 시료수 1: 접미사(-001) 제거
- 시험항목 저장: _isRefTestMode 조건 제거 → 모든 모드에서 저장

---

## 2. 등급 통합 (grade-calculator.js)

### js/grade-calculator.js 공통 모듈 생성
- gradeCalcCompany(): 8개 항목 통합 계산
- gradeRenderBadge(): 등급 뱃지 통일
- gradeResolveDept(): 영업담당 이름→부서 자동 변환
- gradeMatchCondition(): 하위 부서 포함 매칭

### 3페이지 통합
- companyMgmt: 등급 계산 + Firestore 저장 (기준점)
- salesMgmt: Firestore 저장 값 읽기
- receiptStatus: Firestore 저장 값 읽기 (독립 계산 제거)

### 등급규칙 (adminSettings.html)
- 7번 시료이동, 8번 시료접수 추가
- D/F 등급 추가 (6단계)
- 행 추가/삭제 + 배점 비율 자동 조정
- 담당부서 드롭다운 (Firestore users 연동)
- 접수형태/시료이동 옵션 접수등록 연동

---

## 3. 수정이력

### 접수현황 사이드패널 "수정이력" 탭 추가
- editLog 배열로 Firestore 저장
- 35개+ 필드 비교 (상단/업체/발송/청구서/수수료/공지/지부/라벨)
- 시료별 16개 필드 + 시험항목 추가/삭제 항목명 구체적 기록
- 편집 모드 수정 저장 후 자동으로 접수현황 이동

---

## 4. 묶음 관리 개선

### 중복 방지 + 다른 시료 일괄 적용
- 동일 묶음 중복 추가 차단
- 다른 묶음 → 교체 확인 후 기존 제거
- askApplyToOtherSamples(): 공통 함수

### mfdsTestItems 모드 UI 분리
- 품목정보 검증 패널 숨김
- 식품유형 피커/품목코드 숨김
- inspection-source-info 바 숨김
- 제품 선택 시 시험항목 로드 → mfdsTemplates만 허용

---

## 5. PDF OCR Claude Vision

### companyRegForm_v2.html
- 멀티페이지 PDF: Claude Vision으로 분류 + 필드 추출
- 사업자등록증/인허가문서 자동 분류 후 필드 자동 채움
- 인허가 우편번호 자동 검색
- 어느 슬롯에 업로드하든 자동 분류

---

## 6. 수수료 동시분석 그룹

### inspectionMgmt.html + sampleReceiptV2.html
- B30005(총아플라톡신) aflatoxin 그룹 추가
- B30013 명칭: "총 아플라톡신..." → "아플라톡신B1" (Firestore 17건)
- 수수료 표시: 동시분석 중복 → "0원" (취소선 대신 명확)
- 엑셀 다운로드: 중복 0원 + 비고 기록
- 수수료 합계: 묶음 수수료 + 개별 항목 합산 (기존: 패키지로 대체)

---

## 7. PA 참고용(식품) + PB 참고용(축산)

### 상위 카테고리 칩 추가
- REF_CODE_CATEGORIES에 PA/PB 독립 카테고리
- P에서 PA/PB 분리 (P 클릭 시 PA/PB 제외)
- PA0001~PA0477: 477건 시험항목 Firestore 등록
- 수수료 456건 settings/feeData에 등록

### 소비기한 식품유형 임포트
- js/expiry-food-types.js: 156건 식품유형 엑셀 변환
- 📋 식품유형 버튼: 묶음 패널에 추가
- 선택 시 자가품질위탁검사용 코드/단위 매칭
- 단위 추천: mfdsTemplates + 코드별 기본 단위

---

## 8. 기타 수정

- 시험항목명 '||' 단서조항 표시 제거 (receiptStatus, testResultInput)
- 시료접수 '방문' 옵션 추가
- 접수현황 탭 글씨 내려옴 방지 (white-space:nowrap)
- 수수료 검사목적 삭제 시 Firestore 즉시 저장
- 고객사 등록 딜레이 개선
- 제조일자 > 소비기한 에러 토스트
- 상단 헤더바 필드 간격 조정
- 글꼴 설정 적용 시 정렬 개선

---

## Firestore 데이터 등록 확인

| 대상 | 건수 | 컬렉션/문서 |
|------|------|------------|
| PA 시험항목 | 477건 | mfds_test_items (PA0001~PA0477) |
| PA 수수료 | 456건 | settings/feeData |
| B30013 명칭 수정 | 17건 | mfdsTemplates |
| P소분류 A/B | 2건 | settings/pRefSubCategories |
| 등급규칙 | - | settings/gradeRules |
| 업체라벨 | - | settings/companyLabels |
