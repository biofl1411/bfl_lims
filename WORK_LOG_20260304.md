# 작업 일지 — 2026-03-04

## 세션 1 (이전 세션 이어서)

### 1. 시험항목 매핑 시 단위 반영
- `saveMfdsMapping()` 수정: 단위 선택 시 `itemGroupData.항목단위` + 편집 패널 `input-항목단위` 즉시 반영
- `input-시험항목코드`도 함께 반영

### 2. 아코디언 열림 상태 유지
- `_openAccordionTypes` Set + `_saveOpenAccordions()` 함수 추가
- saveItem, deleteItem, executeBulkAdd, copyItemGroupType, deleteItemGroupType, editItemGroupType, deleteItemGroupItem, saveMfdsMapping, deleteMfdsMapping 9개 함수에 적용
- 저장/추가/삭제 후 현재 열린 아코디언이 닫히지 않음

### 3. 데이터 손실 방지 (CRITICAL)
- **원인**: `saveItemGroupsToFirestore()`가 전체 삭제 + 재저장 방식 → Firestore 일일 쓰기 한도(20K) 초과로 5,467건 → 450건 손실
- **복구**: `inspectionFees` 컬렉션(7,481건)에서 6,410건 복원 → 최종 6,869건
- **수정**: 안전장치(50% 미만 저장 중단) + 증분 저장 방식으로 변경

### 4. 단위 자동완성
- 매핑 모달의 단위 `<select>` → 검색 가능한 `<input>` + 드롭다운
- `filterMfdsUnitList()`, `selectMfdsUnit()`, `populateMfdsUnitSelect()`

### 5. 검체유형 선택 삭제
- 아코디언 헤더에 체크박스 추가
- 삭제 바: 전체선택/선택 삭제/취소
- `updateIgDeleteBar()`, `toggleIgSelectAllTypes()`, `clearIgTypeSelection()`, `deleteSelectedIgTypes()`

---

## 세션 2 (현재 세션)

### 6. 참고용 한글명 표시 우선순위 수정
- **문제**: 항목명이 "세균수"인데 testItemCode(A30058=콜레스테롤)의 한글명으로 덮어씀
- **수정**: `항목명`이 한글이면 항목명 우선 표시 (`isKorean` 체크)
- mfdsKor와 다를 경우 영문명 위치에 보조 표시

### 7. 참고용 상세패널 수수료 필드 항상 표시
- **문제**: 수수료 매칭 데이터 있을 때만 표시, 없으면 숨김
- **수정**: 참고용 항목은 수수료 필드 항상 표시 + 편집 가능
- 추가/편집 시 `항목수수료` Firestore 저장
- 테이블 + 아코디언 헤더 합계에 `항목수수료` 반영

### 8. itemGroupData testItemCode 전체 삭제
- 복구 과정에서 inspectionFees에 있던 testItemCode 76건이 딸려옴
- 사용자 요청으로 전체 삭제 → 참고용 칩 0건 상태

### 9. X2(RT-PCR) / X3(Elisa) 항목 복원
- `mfds_test_items`에서 X2/X3 항목 사라진 상태 확인
- itemGroupData의 RT-PCR(21종), Elisa(13종) 기반으로 재생성
- X20001~X20021 (RT-PCR), X30001~X30013 (Elisa) → Firestore 등록

### 10. P코드(참고용 임의지정) 331종 신규 생성 ⭐
- **소스**: `참고용_식품_축산_통합.xlsx` (804행, 331 고유 항목)
- **영문 프리픽스**: P (임의지정 코드)
- **소분류 체계**:
  - P1: 이화학/일반 (73종) — 수분, 산가, pH, Brix, 성상 등
  - P2: 영양성분 (80종) — 단백질, 지방, 탄수화물, 비타민, 미네랄 등
  - P3: 미생물 (82종) — 세균수, 대장균, 살모넬라 등
  - P4: 중금속/유해물질 (18종) — 납, 카드뮴, 수은, 비소 등
  - P5: 식품첨가물 (33종) — 보존료, 타르색소, 인공감미료 등
  - P6: 곰팡이독소 (12종) — 아플라톡신, 오크라톡신 등
  - P9: 기타 (33종) — 방부력시험, 안전성시험 등
- **코드**: `REF_CODE_CATEGORIES`에 P 추가 (색상: #7c4dff)
- **Firestore 등록**: `mfds_test_items` 331건, `settings/refFeeMapping` 331건(수수료 포함)
- **데이터 파일**: `data/p_code_items.json`

---

## 커밋 이력

| 커밋 | 내용 |
|------|------|
| `2aa6379` | 참고용 탭 개선: 단위 로드, 그룹 복사, 기타 분야, BFL 매칭 |
| `da05513` | 시험항목 매핑 시 단위 선택 → 항목단위 자동 반영 |
| `c41d1ba` | 매핑 저장 시 항목단위+시험항목코드 편집 패널에도 즉시 반영 |
| `a23a214` | 참고용 아코디언 열림 상태 유지 |
| `f5fb704` | 데이터 손실 방지 + 단위 자동완성 기능 |
| `3e50517` | 참고용 검체유형 선택 삭제 기능 |
| `aed357d` | 한글명 표시 우선순위 수정 + 수수료 필드 항상 표시 |
| `0c6943b` | 시험항목 탭에 참고용 필터 칩 추가 |
| `5d3e75a` | P코드(참고용) 카테고리 추가: 331종 시험항목 |

---

## Firestore 현황 (2026-03-04 기준)

| 컬렉션 | 건수 | 비고 |
|--------|------|------|
| `mfds_test_items` | ~3,305건 | 기존 2,940 + X2/X3 34 + P코드 331 |
| `settings/refFeeMapping` | 2,531건 | 기존 2,200 + P코드 331 (수수료 포함) |
| `itemGroups` | 2,791건 | testItemCode 전부 삭제됨 |
| `inspectionFees` | 7,481건 | 변경 없음 |

---

## ⚠️ 주의사항 (새 컴퓨터 설정 시)

### Git 설정
- 로컬 브랜치: `master`, 리모트: `main`
- push 명령: `git push origin master:main`
- 또는 새 컴퓨터에서 `git clone` 후 `main` 브랜치로 통일 권장

### SSH 서버
- 새 서버: `ssh biofl@192.168.0.96` (SSH 키 인증 — 새 컴퓨터에서 키 재설정 필요)
- 구 서버: `ssh -p 2222 biofl@14.7.14.31` (pw: bphsk*1411**)
- 배포: `ssh biofl@192.168.0.96 "cd /home/biofl/bfl_lims && git pull origin main"`

### SSH 키 재설정 (새 컴퓨터)
```bash
ssh-keygen -t ed25519 -C "bfl_lims"
ssh-copy-id biofl@192.168.0.96
```

### 필수 도구
- Git
- Python 3 (openpyxl 패키지)
- Node.js (선택)
