# WORK_LOG — 2026-04-02

## 작업 요약

접수 현황 페이지를 부서별로 분리(영업·성적 관리), 자가품질위탁검사용 편집 버그 수정,
처리기한 계산 개선(접수일 포함·휴일 반영·워킹데이 Firestore 관리), 휴일 관리 탭 이동 및 기능 강화.
주요 키워드: **부서별 접수현황**, **자가품질위탁검사 편집 버그**, **처리기한 워킹데이 Firestore 설정**, **휴일 관리**, **일괄 재계산**

---

## 1. 영업 관리 > 접수 현황 신규 추가 (`salesStatus.html`)

### 내용
- `receiptStatus.html` 복제 후 영업 관리용으로 조정
- Topbar: "영업 관리 › 접수 현황"
- "📝 접수등록에서 수정하기" 버튼 제거 (영업 관리에서는 직접 수정 불가)
- 사이드바에 영업 관리 > 접수 현황 메뉴 추가 (`js/sidebar.js`)

커밋: `(이전 세션)`

---

## 2. 성적 관리 > 접수 현황 신규 추가 (`reportDispatch.html`)

### 내용
- `receiptStatus.html` 복제 후 성적 관리용으로 조정
- Topbar: "성적 관리 › 접수 현황"
- 발송 이모지 컬럼 추가 (체크박스 다음 열): 이메일 📧 / 팩스 📠 / 우편 📮 / 선발송 ⚡ 아이콘
  - 해당 발송 수단 선택됐으면 opacity 1, 미선택이면 0.18 (흐리게)
- 처리기한 행 배경 색상:
  - D-2 이하 → 빨강 `#fde8e8`
  - D-5 이하 → 노랑 `#fffde7`
  - D-17 이하 (워킹데이 12일 범위) → 민트 `#e0f7f4`
- 기본 정렬: 처리기한 오름차순 (D-day 가까운 순)
- 사이드바에 성적 관리 > 접수 현황 메뉴 추가

커밋: `(이전 세션)`

---

## 3. 자가품질위탁검사용 편집 모드 버그 수정 (`sampleReceiptV2.html`)

### 증상
접수 현황에서 "접수등록에서 수정하기" → 검사목적이 자가품질위탁검사용인 접수건 진입 시,
토스트 메시지 발송 후 시험항목 목록이 사라짐

### 원인
`updateInspectionMode()` 내 2초 딜레이 실행 코드가 편집 모드에서도 `foodTypeInput.value = ''`와
`inspectionItemsEl.style.display = 'none'`을 실행

### 수정
`!window._editDocId` 조건 추가 — 편집 모드(`window._editDocId` 존재)일 때는 초기화 차단

```javascript
if (foodTypeInput && !window._editDocId) foodTypeInput.value = '';
if (inspectionItemsEl && !window._editDocId) inspectionItemsEl.style.display = 'none';
```

커밋: `(이전 세션)`

---

## 4. 처리기한 계산 개선 (`sampleReceiptV2.html`)

### 4-1. 접수일 포함 계산
기존: 접수일 다음날부터 카운트 → 접수일을 포함해서 카운트로 변경

```javascript
// 변경 전: let added = 0
// 변경 후:
let added = isWorkingDay(date) ? 1 : 0; // 접수일이 워킹데이면 1일로 카운트
```

### 4-2. Firestore 휴일 반영
기존: `getHolidays()`가 localStorage 읽음 → 실제 휴일 설정이 Firestore에 있어 미반영
수정: `loadHolidaysFromFirestore()` 함수 추가, `firebase-ready` 이벤트 후 로드

```javascript
var _holidayDatesCache = null;
async function loadHolidaysFromFirestore() {
    var fsData = await fsGetSettings('holidays');
    // dates 배열 또는 list[].date 형식 모두 처리
}
```

### 4-3. 워킹데이 수 Firestore 관리
기존: 하드코딩 `12` → `_deadlineWorkingDays` 변수로 대체

```javascript
var _deadlineWorkingDays = 12; // 기본값
async function loadDeadlineWorkingDays() {
    var data = await fsGetSettings('deadlineRule');
    if (data && data.workingDays) _deadlineWorkingDays = parseInt(data.workingDays) || 12;
}
// firebase-ready 이벤트 후 로드
// calculateProcessingDeadline()에서 _deadlineWorkingDays 사용
```

커밋: `a917390` (일부)

---

## 5. 휴일 관리 탭 이동 및 개편 (`receiptSettings.html`)

### 5-1. 탭 이동
관리자 설정에 있던 "휴일 관리"를 접수 관리 > 설정으로 이동

### 5-2. 탭명 변경
"📅 휴일 관리" → "📅 처리기한 및 휴일관리"

### 5-3. 처리기한 설정 UI 추가
탭 상단에 "⏱️ 처리기한 설정" 섹션 추가

| 항목 | 내용 |
|---|---|
| Firestore 위치 | `settings/deadlineRule` 문서, `workingDays` 필드 |
| UI | 숫자 입력 (1~60) + 저장 버튼 |
| 기본값 | 12일 (설정 없을 때) |
| 로드 시점 | 탭 진입 시 자동 |

### 5-4. 처리기한 일괄 재계산 개선
- `not-in` 쿼리 → 전체 조회 후 JS 필터링으로 변경 (Firestore compat SDK 호환성 우회)
- 워킹데이 수도 Firestore `deadlineRule`에서 로드하여 사용 (하드코딩 12 제거)
- 상태 메시지 상세화: 워킹데이 수, 휴일 건수, 처리 진행 현황, 최종 업데이트 건수

커밋: `9296167`, `a917390`

---

## 커밋 이력

| 커밋 | 파일 | 내용 |
|---|---|---|
| `(이전 세션)` | salesStatus.html, sidebar.js | 영업 관리 접수 현황 추가, 수정 버튼 제거 |
| `(이전 세션)` | reportDispatch.html, sidebar.js | 성적 관리 접수 현황 추가, 발송 이모지·색상·정렬 |
| `(이전 세션)` | sampleReceiptV2.html | 자가품질위탁검사용 편집 모드 버그 수정 |
| `(이전 세션)` | sampleReceiptV2.html | 처리기한 접수일 포함·휴일 Firestore 반영 |
| `(이전 세션)` | receiptSettings.html | 휴일 관리 탭 이동, 일괄 재계산 버튼 구현 |
| `9296167` | receiptSettings.html | not-in 쿼리 우회, 상태 메시지 상세화 |
| `a917390` | receiptSettings.html, sampleReceiptV2.html | 처리기한 워킹데이 Firestore 설정 관리 |

---

## 이월 작업

- **처리기한 일괄 재계산 검증**: 실서버에서 버튼 클릭 후 처리기한 변경 여부 확인
- **testDiary.html 섹션·필드 드래그 재배치**: 이전 세션에서 구현 완료, 동작 검증 필요
- **납 기록서 분석조건 표 직접 입력** (이월)
- **결과 입력 납 상세폼 검증** (이월)
- **paymentMgmt.html 전면 재구성** (이월)
- **PA/PB mfds_test_items 복구** (이월)
- **거래내역서 인쇄 계좌 정보 동적 연동** (이월)
